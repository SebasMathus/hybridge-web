import { BenefitsHybridgeGrid } from '@/components/BenefitsHybridgeGrid'

/** Bloque CMS: grid de beneficios con logos de aliados. */
export function BenefitsHybridgeBlockComponent() {
  return <BenefitsHybridgeGrid />
}
